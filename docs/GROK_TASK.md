# GROK TASK

**Status: WAIT**

Do not modify code yet.

Reason: Stage 0 contracts are being bootstrapped and must be reviewed/merged before Grok creates an implementation branch.

When status changes to READY, branch from the exact base commit specified here. Grok owns only `feature/adult/**` unless the task explicitly says otherwise. Changes to `core/adult-contracts/**` require coordination first.
