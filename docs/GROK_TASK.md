# GROK TASK

**Status: WAIT**

G-006 is complete, reviewed and merged to `main`.

Do not repeat G-006 and do not start a new Grok branch yet.

ChatGPT is currently implementing the shared deterministic `core:scene` composer and character-card/UI integration, including explicit adult-only `Роздягнути` / `Раздеть` state, morphology-aware rig compatibility, deterministic fallbacks and asset-pack handoff.

The next Grok task will be **G-007** after the shared scene-composer surface is merged to `main` and stable. G-007 will connect the adult visual recipe/content-pack layer to that shared scene interface without reducing the accepted adult-content catalogue or intensity.

Until `Status: READY` appears here again, Grok should wait and make no repository changes.

Do not run GitHub Actions.
